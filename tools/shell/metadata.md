---
name: shell
tag: system
description: "Run a shell command in the project workspace. Read-only commands auto-approved, write commands require confirmation."
---

# shell
Runs arbitrary shell commands in a sandboxed workspace directory.
Use for file operations, downloads, data processing — anything without a dedicated tool.
