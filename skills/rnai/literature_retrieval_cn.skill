---
name: literature_retrieval_cn
category: rnai
description: 从本地 RAG 知识库和在线数据库（PubMed/OpenAlex）检索 RNAi 文献，提取实验参数，生成结构化证据报告的分步指南。
keywords: [文献检索, pubmed, openalex, rag, rnai记录, 知识检索, 证据报告]
required_tools_tag: [discovery, literature_search]
---

# 文献检索指南

## 概述

本指南涵盖了 RNAi 农药研究的完整文献检索工作流。它整合了本地 RAG 知识库查询与在线 PubMed/OpenAlex 搜索，用于收集、分析和报告 RNAi 实验证据。除非用户明确拒绝在线检索，否则默认始终执行在线搜索。

工作流包含五个阶段：
1. **输入分析与策略优化** — 解析用户输入，确定搜索范围
2. **本地 RAG 知识库查询** — 搜索 `rnai_records` 获取结构化实验记录
3. **在线文献检索** — 搜索 PubMed 和 OpenAlex 获取最新发表文献
4. **内容分析与提取** — 从所有来源提取关键实验参数
5. **报告生成** — 生成结构化的、附带引用的证据报告

---

## 阶段 1：输入分析与策略优化

### 任务

分析用户输入，确定最优搜索策略。在搜索前将俗名转换为拉丁学名。

### 策略选择

| 用户输入模式 | 策略 | 示例 |
|-----------|------|------|
| **情况 A — 精确搜索**：同时提供了害虫种名和靶标基因 | 直接定向检索 | "Spodoptera frugiperda 的几丁质合成酶 RNAi" |
| **情况 B — 模糊搜索**：仅提供了害虫种名或生物学描述 | 该物种的宽泛 RNAi 关键词搜索 | "Spodoptera frugiperda 的 RNAi" 或 "Spodoptera 致死 RNAi 靶标" |
| **情况 C — 扩展搜索**：靶标基因已确定，但该物种无记录 | 向分类学上游扩展（属 → 科 → 目） | "CHS1 RNAi 鳞翅目" |

### 名称转换规则

如果用户提供的是俗名（如"秋粘虫"、"家蚕"、"稻飞虱"），在进行任何搜索前必须转换为标准的拉丁双名法名称：

| 俗名 | 拉丁学名 |
|------|---------|
| 秋粘虫（草地贪夜蛾） | _Spodoptera frugiperda_ |
| 家蚕 | _Bombyx mori_ |
| 稻飞虱（褐飞虱） | _Nilaparvata lugens_ |
| 烟草天蛾 | _Manduca sexta_ |
| 赤拟谷盗 | _Tribolium castaneum_ |
| 黄热病蚊（埃及伊蚊） | _Aedes aegypti_ |

### 质量门

- 在进入阶段 2 前，确认拉丁学名是有效的。
- 如果用户输入不明确，在确定搜索策略前先提出澄清性问题。

---

## 阶段 2：本地 RAG 知识库查询

### 任务

搜索本地 `rnai_records` 知识库，获取匹配目标物种和/或基因的结构化实验记录。

### 工具：`search_knowledge`

**参数：**
- `query`：拉丁学名加相关关键词（如 `"Spodoptera frugiperda chitin synthase RNAi"`）。对于模糊搜索，使用种名加 `"RNAi"`（如 `"Spodoptera frugiperda RNAi"`）。
- `knowledge_base`：`"rnai_records"`
- `top_k`：10（广泛探索时）；3-5（特定基因查找时）
- `semantic_weight`：使用知识库推荐默认值（0.4）

**输出含义：**
每条返回的记录通常包含：
- `species`：研究的昆虫物种
- `target_gene`：RNAi 靶向的基因
- `primer_sequences` 或 `dsRNA_sequence`：使用的序列
- `phenotype`：观察到的表型效应
- `mortality_rate`：报告的死亡率百分比
- `delivery_method`：dsRNA 递送方式（注射、饲喂、浸泡等）
- `developmental_stage`：应用 RNAi 的发育阶段
- `interference_efficiency`：测量的敲低效率
- `vector`：使用的递送载体（如有）
- `reference`：来源文献引用

### 结果处理

- **记录充足**（≥5 条相关记录）：进入阶段 4（内容分析）。仅当用户未要求最新发表文献时，可跳过阶段 3（在线检索）。
- **记录较少或无记录**（<5 条相关记录）：始终进入阶段 3 进行在线检索。
- **完全没有记录**：向用户报告此情况，然后进入阶段 3。

### 边界情况：知识库不可用

如果 `search_knowledge` 返回 Chroma 索引缺失或知识库不存在的错误：
1. 告知用户本地知识库不可用。
2. 直接进入阶段 3（在线文献检索）。
3. 在最终报告中注明本地 RAG 知识库不可用。

---

## 阶段 3：在线文献检索

### 任务

搜索在线数据库（PubMed 和 OpenAlex）获取相关出版物。此阶段**默认始终执行**，除非用户明确表示不需要在线搜索。

### 步骤 3a：PubMed 搜索

使用 `pubmed_Esearch` 查找相关的 PMID，然后使用 `pubmed_Efetch` 获取详细信息。

#### 工具：`pubmed_Esearch`

**参数：**
- `query`：简化关键词——从具体查询开始，无结果时放宽范围。使用拉丁学名，不要用俗名。
  - 示例：`"RNAi Spodoptera frugiperda chitin synthase"`
  - 放宽后：`"RNAi Spodoptera frugiperda"` 或 `"RNAi Lepidoptera"`
- `max_results`：10-20（默认 10）
- `min_year`：可选的年份过滤（如 `"2020"`）
- `max_year`：可选的年份过滤（如 `"2025"`）

**输出含义：**
- `pmids`：匹配查询的 PubMed ID 列表
- `total_count`：找到的结果总数
- 如果 `pmids` 为空，表示查询未返回结果——需要放宽搜索词

#### 工具：`pubmed_Efetch`

**参数：**
- `pmids`：来自 `pubmed_Esearch` 输出的逗号分隔的 PMID 列表

**输出含义：**
每条返回的文章包含：
- `pubmed_id`：PMID
- `title`：文章标题
- `abstract`：文章摘要
- `doi`：数字对象标识符
- `publication_year`：出版年份
- `concepts`：MeSH 术语或关键词（最多 6 个）

### 步骤 3b：OpenAlex 搜索（互补）

使用 `openalex_search` 作为互补搜索。OpenAlex 免费使用，无需 API 密钥，并提供引用次数用于评估文章影响力。

#### 工具：`openalex_search`

**参数：**
- `keyword`：搜索关键词（与 PubMed 相同策略）
- `max_results`：10-20
- `field`：可选的领域过滤（如 `"insect"`、`"molecular biology"`、`"agriculture"`）
- `min_year`：可选的年份过滤
- `max_year`：可选的年份过滤

**输出含义：**
每条返回的文章包含：
- `title`：文章标题
- `abstract`：重建的摘要文本
- `doi`：DOI 链接
- `publication_year`：出版年份
- `cited_by_count`：引用次数（用于评估影响力）
- `concepts`：研究概念标签（最多 6 个）

### 结果过滤

审查所有返回文章的元数据，过滤不相关的结果：
- 仅保留与**昆虫 RNAi** 严格相关的出版物（不包括哺乳动物 RNAi、植物 RNAi、siRNA 治疗）
- 去除 PubMed 和 OpenAlex 结果中的重复项
- 优先选择有摘要的文章（它们包含可用的实验细节）

### 无结果处理

如果 PubMed 和 OpenAlex 都未返回结果：
1. 放宽搜索范围（使用更少的关键词，扩展到属/科级别）。
2. 如果仍然没有结果，告知用户并建议替代搜索词或物种。
3. **不要**编造或虚构参考文献。

---

## 阶段 4：内容分析与提取

### 任务

从所有检索来源（RAG 记录 + 在线文章）中提取关键实验参数。整理提取的数据用于最终报告。

### 提取模板

对于每条检索到的文章或记录，提取以下字段：

| 字段 | 描述 | 示例 |
|------|------|------|
| **文献来源** | PMID、DOI、标题、期刊、出版年份 | PMID: 12345678, DOI: 10.xxx/xxxx |
| **靶标详情** | 靶标基因名称、功能描述 | CHS1，几丁质合成酶，几丁质代谢 |
| **实验设计** | RNAi 递送方法、dsRNA 浓度、实验设置 | 注射，500 ng/µL，3 龄幼虫 |
| **RNAi 结果** | 死亡率、生长抑制率、表型变化 | 7 天死亡率 85%，蜕皮异常 |
| **结论** | 干扰效率、关键研究发现 | 显著敲低（>80%），致死表型 |

### 组织方式

- **按物种分组**：如果搜索了多个物种，按物种组织结果。
- **按靶标基因分组**：在每个物种内，按靶标基因分组。
- **去重**：去除重复记录（同一 PMID/同一研究同时出现在 RAG 和在线结果中）。

---

## 阶段 5：报告生成

### 任务

生成结构化的、附带引用的证据报告，涵盖完整的检索过程和所有发现。

### 报告结构

**1. 搜索策略**
- 目标物种和/或基因
- 使用的搜索词（RAG、PubMed、OpenAlex）
- 日期范围过滤（如有）
- 搜索的数据库数量

**2. 本地 RAG 结果摘要**
- 知识库：`rnai_records`
- 找到的记录数量
- 按物种或按基因的摘要表
- 如知识库不可用则注明

**3. 在线文献结果**
- PubMed 结果：找到的 PMID 数量，获取的详细信息
- OpenAlex 结果：找到的文章数量
- 过滤摘要（相关性筛选后保留的数量）
- 每篇文章表格：PMID、标题、期刊、年份、DOI、关键发现

**4. 实验参数摘要**

整理为结构化表格：

| 物种 | 靶标基因 | 递送方法 | 浓度 | 死亡率 | 表型 | 发育阶段 | 引用 |
|------|---------|---------|------|--------|------|---------|------|
| _S. frugiperda_ | CHS1 | 注射 | 500 ng/µL | 85% | 蜕皮异常 | 幼虫 | PMID: xxx |
| _S. frugiperda_ | V-ATPase | 饲喂 | 100 µg/mL | 65% | 生长停滞 | 幼虫 | PMID: yyy |

**5. 结论与建议**
- 找到的最强 RNAi 证据摘要
- 哪些靶标基因具有最佳的死亡率/效率数据
- 对研究人员的后续步骤建议
- 如果未找到相关文献，明确说明

### 词汇规则

**允许使用：**
- 提示、可能、候选、已报道、观察到
- "CHS1 已被报道为 _S. frugiperda_ 的有效 RNAi 靶标，死亡率达 85%"
- "这些发现提示 V-ATPase 可能是一个有前景的靶标"

**禁止使用：**
- 证明、证实、优越、保证、确认（除非直接引用原文结论）
- 绝不断言检索文献中未明确报告的实验结果

### 边界情况：未找到结果

如果在任何来源中均未找到相关文献：
1. 明确说明："在搜索的数据库中未找到 [物种/基因] 的 RNAi 文献。"
2. 建议放宽搜索范围（如扩展到属或科级别）。
3. **不要**生成虚构的参考文献或编造实验参数。

---

## 完整工作流示例

```
用户："Spodoptera frugiperda 有哪些 RNAi 靶标被研究过？"

Agent 执行步骤：
1. 阶段 1 — 输入分析
   输入：species="Spodoptera frugiperda"（已是拉丁名），无特定基因
   策略：情况 B（模糊搜索）— 宽泛 RNAi 关键词搜索

2. 阶段 2 — RAG 查询
   search_knowledge(query="Spodoptera frugiperda RNAi",
                    knowledge_base="rnai_records", top_k=10)
   → 返回 8 条记录（CHS1, V-ATPase, HSP70 等）

3. 阶段 3 — 在线检索（默认始终执行）
   pubmed_Esearch(query="RNAi Spodoptera frugiperda", max_results=10)
   → 返回 10 个 PMID
   pubmed_Efetch(pmids="123,124,125,...")
   → 返回完整的文章详情
   openalex_search(keyword="RNAi Spodoptera frugiperda", max_results=10)
   → 返回 8 篇文章及引用次数

4. 阶段 4 — 内容分析
   合并 RAG + 在线结果，去重，提取参数

5. 阶段 5 — 报告生成
   生成包含所有章节的结构化报告
```

## 计算来源参考

| 组件 | 方法 |
|------|------|
| RAG 知识检索 | Chroma 向量数据库 + BM25 混合搜索（BAAI/bge-large-en-v1.5） |
| PubMed 搜索 | NCBI E-utilities API（ESearch + EFetch） |
| OpenAlex 搜索 | OpenAlex REST API（免费，无需认证） |
| 内容提取 | 从检索记录中由 LLM 综合提取 |
| 报告生成 | LLM 综合生成 |
