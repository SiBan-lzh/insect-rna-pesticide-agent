---
name: experiment_design_cn
category: rnai
description: 利用 experiment_protocol 和 rnai_records 知识库设计昆虫 RNAi 湿实验方案的分步指南，可选在线文献检索补充实验参数参考。
keywords: [实验, 方案, 湿实验, rnai, dsrna合成, 递送, qpcr, 验证]
required_tools_tag: [discovery, literature_search]
---

# 实验设计指南

## 概述

本指南涵盖如何设计完整的昆虫 RNAi 湿实验方案。它整合了**三个信息来源**来生成基于证据的、物种特异性的实验设计：

1. **`experiment_protocol` KB** — 31 份精心整理的昆虫 RNAi 实验方案，提供分步程序、设备需求和方案逻辑
2. **`rnai_records` KB** — 4245 条真实实验记录，提供具体的参数值（发育阶段、dsRNA 浓度、寄主植物、递送方法、死亡率等）
3. **在线文献检索**（PubMed / OpenAlex）— 可选，当 KB 证据不足或用户要求最新发表参数时使用

工作流包含五个阶段：
1. **需求分析与多源 KB 搜索** — 确定实验约束，搜索两个 KB，可选在线检索
2. **生理背景分析** — 确定昆虫饲养条件和最佳干预窗口
3. **核酸与配方设计** — 设计 dsRNA 合成和配方制备步骤
4. **递送与暴露规划** — 详述递送参数、剂量和暴露持续时间
5. **验证流程与对照** — 设计 qPCR 验证、表型观察和对照组

---

## 阶段 1：需求分析与多源 KB 搜索

### 任务

从用户输入中确定实验约束，然后搜索 **`rnai_records`**（获取具体实验参数）和 **`experiment_protocol`**（获取程序性工作流逻辑）。当 KB 证据不足时，可选执行在线文献检索。

### 步骤 1a：确定实验约束

提取或询问用户以下关键约束：

| 约束 | 描述 | 示例 |
|------|------|------|
| **目标物种** | 昆虫物种（拉丁学名） | _Spodoptera frugiperda_ |
| **靶标基因** | 被敲低的基因 | CHS1、V-ATPase |
| **发育阶段** | 应用 RNAi 的阶段 | 卵、幼虫（龄期）、蛹、成虫 |
| **递送方法** | dsRNA 的递送方式 | 注射、饲喂、浸泡、涂抹 |
| **实验规模** | 每组大约昆虫数量 | 每组 30-50 只 |

如果用户未提供所有约束，使用已有的信息继续——KB 搜索仍然可以找到部分匹配。

### 步骤 1b：搜索 `rnai_records` 获取实验参数

首先搜索 `rnai_records` 知识库——它包含 4245 条真实实验记录，具有可直接用于定制设计的参数值。

#### 工具：`search_knowledge`（rnai_records）

**参数：**
- `query`：拉丁学名 + 靶标基因 + 递送方法（如已知）。使用技术性英文关键词。
  - 示例：
    - `"Spodoptera frugiperda CHS1 injection dsRNA concentration"`
    - `"Tribolium castaneum V-ATPase feeding mortality"`
    - `"Nilaparvata lugens RNAi developmental stage instar"`
    - `"Bombyx mori dsRNA delivery host plant"`
- `knowledge_base`：`"rnai_records"`
- `top_k`：10-15（最大化每物种的参数覆盖）
- `semantic_weight`：使用知识库推荐默认值（0.4）

**输出含义：**
每条返回的记录是包含可操作参数值的真实实验条目：
- `developmental_stage`：使用的确切龄期或阶段（如"3龄幼虫"、"成虫"）
- `delivery_method`：dsRNA 的递送方式
- `concentration`：使用的 dsRNA 浓度（如"500 ng/µL"、"100 µg/mL diet"）
- `dosage`：每只昆虫的剂量（如"每只幼虫 1 µg"）
- `host_plant`：饲喂实验中使用的寄主植物
- `vector`：使用的递送载体（如"壳聚糖纳米颗粒"）
- `mortality_rate`：观察到的死亡率（对功效分析有用）
- `interference_efficiency`：报告的敲低效率
- `phenotype`：观察到的表型效应
- `reference`：来源出版物

**优先级规则：** 从 `rnai_records` 中提取的与用户目标**相同物种**的参数具有最高优先级。如果用户的确切物种没有记录，使用最近亲缘物种（同属或同科）的记录。

### 步骤 1c：搜索 `experiment_protocol` 获取工作流逻辑

在获取参数值后，搜索 `experiment_protocol` KB 获取完整的程序性工作流逻辑——饲养方案、合成步骤、验证流程。

#### 工具：`search_knowledge`（experiment_protocol）

**参数：**
- `query`：根据约束生成技术性英文关键词。关注程序和技术的名称。
  - 示例：
    - `"Spodoptera frugiperda larval injection dsRNA protocol"`
    - `"dsRNA synthesis in vitro transcription T7 purification"`
    - `"RNA extraction qPCR validation insect RNAi"`
    - `"chitosan nanoparticle dsRNA formulation"`
- `knowledge_base`：`"experiment_protocol"`
- `top_k`：5-10（探索性搜索，覆盖多个方案方面）
- `semantic_weight`：使用知识库推荐默认值（0.4）

**输出含义：**
此 KB 使用 `parent_child` 块策略：
- **子块（child chunks）**：具体的程序步骤或参数章节
- **父内容（parent content）**：来自外层章节的更宽泛上下文

每条结果可能包含：
- `content`：具体的协议章节
- `parent_content`：更宽泛的章节上下文
- `source`：精选集合中的文档名称

### 步骤 1d：多源知识综合

合并两个 KB 的结果，构建完整图景：

| 方面 | 主要来源 | 目的 |
|------|---------|------|
| **发育阶段** | `rnai_records` | 该物种真实实验中使用的阶段 |
| **dsRNA 浓度/剂量** | `rnai_records` | 已证明有效的浓度 |
| **寄主植物/饲料** | `rnai_records` | 成功实验中使用的寄主植物 |
| **递送方法详情** | 两者 | 交叉验证；`rnai_records` 提供参数，`experiment_protocol` 提供程序 |
| **饲养条件** | `experiment_protocol` | 温度、湿度、光周期 |
| **合成方案步骤** | `experiment_protocol` | IVT 试剂盒、纯化方法、质控标准 |
| **验证流程** | `experiment_protocol` | RNA 提取、qPCR、内参基因 |
| **表型观察** | 两者 | `rnai_records` 提供预期结果，`experiment_protocol` 提供评分方法 |

### 步骤 1e：可选在线文献检索

在以下情况下执行在线文献检索：
- 用户明确要求最新发表的参数
- 两个 KB 对用户的特定物种或基因返回的结果不足
- 用户希望将其设计与最新发表进行比较

#### 工具：`pubmed_Esearch` + `pubmed_Efetch` / `openalex_search`

**参数：**
- `query`：拉丁学名 + 靶标基因 + "RNAi" + 参数关键词
  - 示例：
    - `"Spodoptera frugiperda CHS1 RNAi injection concentration 2023"`
    - `"Tribolium castaneum dsRNA feeding nanoparticle"`
- `max_results`：5-10
- 可选按年份范围过滤（如 `min_year: "2020"`）以获取近期发表

**从在线文章中提取的内容：**
- 递送方法和剂量参数
- 使用的发育阶段
- 观察到的死亡率/敲低效率
- 任何新颖的配方或递送技术

### 结果处理

- **两个 KB 结果都丰富**（覆盖物种、基因和递送方法）：使用 KB 数据作为主要参考。在线搜索可选。
- **`rnai_records` 有良好的参数，`experiment_protocol` 部分覆盖**：以 `rnai_records` 参数为基础；用一般最佳实践补充缺失的程序步骤。
- **`rnai_records` 稀疏，`experiment_protocol` 有相关方案**：使用 `rnai_records` 中亲缘物种的参数，通过 `experiment_protocol` 工作流逻辑适配到目标物种。
- **两个 KB 都不足**：执行在线文献搜索。如果仍然不足，提供合理估计并明确标注。

### 边界情况：知识库不可用

如果 `search_knowledge` 返回 Chroma 索引缺失的错误：
1. 告知用户本地知识库不可用。
2. 提供在线文献搜索作为备选方案。
3. 如果用户同意，执行 PubMed/OpenAlex 搜索。
4. 如果用户拒绝，使用一般昆虫 RNAi 实验设计原则，并明确标注所有参数为估计值。

---

## 阶段 2：生理背景分析

### 任务

确定目标昆虫的发育特征和饲养需求。根据靶标基因的表达谱确定最佳干预窗口。

### 昆虫饲养条件

从 KB 提取或提供一般指导：

| 参数 | 典型范围 | 说明 |
|------|---------|------|
| **温度** | 25-28°C（大多数昆虫）；30-32°C（部分热带物种） | 因物种而异；查阅文献 |
| **湿度** | 50-70% RH | 防止小型幼虫干燥 |
| **光周期** | 14L:10D 或 16L:8D（大多数）；24D（土壤昆虫） | 匹配物种的自然光周期 |
| **饲料** | 人工饲料（小麦胚芽基、琼脂基）或天然宿主 | 饲料成分影响 RNAi 效率 |

### 最佳干预窗口

通过交叉参考两个来源确定干预窗口：

**优先级 1 — `rnai_records` 的发育阶段数据：**
检查检索到的 `rnai_records` 条目中用户目标物种的记录。使用成功实验中最常报告的发育阶段。如果报告了多个阶段，将其作为选项呈现，附带相应的死亡率/效率数据。

**优先级 2 — 生物学推理（如果 `rnai_records` 没有物种特异性数据）：**
- **基因表达高峰期**：在表达高峰期之前或之时应用 RNAi
- **昆虫大小**：较大的龄期更容易注射，但可能具有更强的免疫反应
- **蜕皮周期**：参与蜕皮的基因（如 CHS、RXR、EcR）最好在早期龄期靶向
- **取食活动**：对于口服递送，确保昆虫正在活跃取食

**示例——使用 `rnai_records` 数据：**
> 从 `rnai_records`：CHS1 在 _S. frugiperda_ **3 龄幼虫**通过注射成功靶向（85% 死亡率）。另一研究使用 **4 龄幼虫**通过饲喂（65% 死亡率）。推荐干预：**3-4 龄幼虫**，注射为首选递送方式以获得更高死亡率。

---

## 阶段 3：核酸与配方设计

### 任务

设计 dsRNA 合成工作流和配方制备步骤。

### 步骤 3a：dsRNA 模板制备

| 步骤 | 详情 |
|------|------|
| **模板 PCR** | 使用带 T7 启动子悬臂的基因特异性引物扩增靶标区域（300-600 bp） |
| **T7 启动子序列** | `TAATACGACTCACTATAGGG`（附加到正向引物 5' 端，或双向转录时附加到两个引物） |
| **PCR 纯化** | 柱纯化或乙醇沉淀；用无核酸酶水洗脱 |
| **模板验证** | 凝胶电泳确认单一条带；可选：Sanger 测序 |

### 步骤 3b：体外转录（IVT）

| 参数 | 典型范围 |
|------|---------|
| **IVT 试剂盒** | T7 RNA 聚合酶（商业试剂盒：MEGAscript、T7 RiboMAX 或 HiScribe） |
| **孵育** | 37°C 2-4 小时（或过夜以提高产量） |
| **DNase 处理** | 加入 TURBO DNase I（或等效物）去除模板 DNA，37°C，15 分钟 |
| **纯化** | LiCl 沉淀、柱纯化或酚氯仿提取 |
| **洗脱缓冲液** | 无核酸酶水或 TE 缓冲液 |
| **质量检查** | Nanodrop（260/280 ≥ 1.8）；凝胶电泳确认大小 |

### 步骤 3c：dsRNA 配方

**优先级 1 — 使用 `rnai_records` 中的浓度数据（如有）：**
检查检索到的 `rnai_records` 条目中用户目标物种和递送方法的记录。提取成功实验中使用的确切浓度、剂量和配方。

| 递送方法 | 配方 | 典型浓度 |
|---------|------|---------|
| **注射** | dsRNA 溶于无核酸酶 PBS 或生理盐水 | 1-10 µg/µL 储备液；每只昆虫 0.1-5 µg |
| **饲喂** | dsRNA 混入人工饲料或涂在叶片上 | 20-200 µg/mL 饲料；50-500 ng/mg 饲料 |
| **浸泡** | dsRNA 溶于培养基 | 0.1-10 µg/mL 培养基 |
| **纳米颗粒** | dsRNA 与壳聚糖、碳纳米管或脂质体复合 | N/P 比 5-20；粒径 100-300 nm |

**示例——使用 `rnai_records` 数据：**
> 从 `rnai_records`：CHS1 dsRNA 以 **500 ng/µL**（每只幼虫 1 µL）注射在 _S. frugiperda_ 中实现 85% 死亡率。V-ATPase dsRNA 以 **100 µg/mL 饲料** 饲喂实现 65% 死亡率。推荐用户实验剂量：**每只幼虫 500 ng-1 µg（注射）** 或 **100 µg/mL 饲料（饲喂）**。

如果 KB 包含物种特异性配方方案（如壳聚糖纳米颗粒制备），提取并呈现这些细节作为主要推荐。根据具体物种调整浓度——如果用户物种与 KB 记录不同，在可行的情况下按体重比例缩放剂量。

---

## 阶段 4：递送与暴露规划

### 任务

详述递送程序、剂量参数和暴露持续时间。

### 来自 `rnai_records` 的递送参数

检查检索到的 `rnai_records` 条目中用户目标物种的记录。提取使用的确切递送参数：

| 参数 | 在 `rnai_records` 中的位置 |
|------|--------------------------|
| **发育阶段** | `developmental_stage` 字段——使用最常报告的阶段 |
| **递送方法** | `delivery_method` 字段——注射、饲喂、浸泡等 |
| **dsRNA 浓度** | `concentration` 字段——确切的 µg/µL 或 µg/mL 值 |
| **每只昆虫剂量** | `dosage` 字段——每只昆虫的 µg 数 |
| **寄主植物** | `host_plant` 字段——对于饲喂实验，使用报告的寄主植物 |
| **死亡率** | `mortality_rate` 字段——对样本量功效分析有用 |
| **敲低效率** | `interference_efficiency` 字段——预期验证结果 |

**示例——使用 `rnai_records` 数据：**
> 从 `rnai_records`：对于 _S. frugiperda_ CHS1 注射——**3 龄幼虫**，**1 µg dsRNA** 溶于 **500 nL PBS**，腹部节间注射，**7 天死亡率 85%**。对于 _S. frugiperda_ V-ATPase 饲喂——**4 龄幼虫**，**100 µg/mL 饲料** 在 **玉米叶片** 上，**10 天死亡率 65%**。

---

## 阶段 5：验证流程与对照

### 任务

设计下游分子验证流程、表型观察指标和对照组。

### 步骤 5a：RNA 提取与 cDNA 合成

| 步骤 | 详情 |
|------|------|
| **取样时间** | 递送后 24-72 小时（取决于基因和递送方法）；条件允许时包含多个时间点 |
| **组织** | 全虫（小型物种）或靶标组织（脂肪体、中肠、表皮） |
| **RNA 提取** | TRIzol 法或柱纯化法（RNeasy）；推荐 DNase I 处理 |
| **RNA 质控** | Nanodrop（260/280 ≥ 2.0）；凝胶或 Bioanalyzer 检测完整性 |
| **cDNA 合成** | 随机六聚体或 oligo-dT；商业试剂盒（iScript、SuperScript 或等效产品） |
| **cDNA 稀释** | 用无核酸酶水稀释 5-10 倍作为 qPCR 模板 |

### 步骤 5b：qPCR 验证

| 参数 | 指导 |
|------|------|
| **内参基因** | 2-3 个稳定的管家基因（actin、GAPDH、RPL18、EF1α、18S rRNA） |
| **引物设计** | 80-200 bp 扩增子；Tm 58-62°C；GC 40-60%；尽可能跨内含子 |
| **qPCR 试剂** | SYBR Green（PowerUp、SYBR Premix Ex Taq 或等效产品） |
| **热循环方案** | 95°C 2 min；[95°C 15 s → 60°C 30 s] × 40 循环；熔解曲线 65-95°C |
| **归一化** | ∆∆Cq 法；对照组均值归一化为 1.0 |
| **重复** | 至少 3 个生物学重复 × 3 个技术重复 |

### 步骤 5c：表型观察

| 指标 | 方法 | 典型时间线 |
|------|------|---------|
| **死亡率** | 每日计数；Kaplan-Meier 生存分析 | 递送后 3-14 天 |
| **生长抑制** | 体重增加（mg/天）；体长 | 3-10 天 |
| **蜕皮缺陷** | 观察蜕皮残留、双头壳 | 2-7 天（预期蜕皮时） |
| **取食抑制** | 饲料消耗量（重量）；虫粪产量 | 每日 |
| **发育延迟** | 化蛹/羽化时间 | 数天至数周 |

### 步骤 5d：对照组设计

| 对照类型 | 目的 | 处理 |
|---------|------|------|
| **阴性对照（模拟）** | 注射/饲喂效应的基线 | 仅缓冲液或无核酸酶水 |
| **非靶标 dsRNA** | 脱靶效应基线 | dsGFP（绿色荧光蛋白）或 dsLuc（荧光素酶）——两者在昆虫中不存在 |
| **乱序 siRNA** | 序列特异性效应确认 | 靶标 siRNA 的乱序版本（相同 GC%，随机顺序） |
| **阳性对照** | 验证 RNAi 通路功能正常 | 对该物种已知有效的 dsRNA（如有） |

### 步骤 5e：一致性检查

在最终确定设计前，验证：
1. **可行性**：所有步骤是否可在现有设备上执行？
2. **时间线**：时间点对于昆虫的发育速率是否现实？
3. **对照**：是否包含所有必要的对照？
4. **样本量**：每组是否 ≥ 3 个生物学重复？
5. **证据支持**：选择的参数是否有 KB 证据或合理估计支持？

---

## 报告生成

### 任务

生成包含所有章节的完整实验设计报告。

### 报告结构

**1. 实验约束摘要**
- 目标物种、基因、阶段、递送方法
- 使用的知识库来源

**2. 昆虫饲养方案**
- 温度、湿度、光周期、饲料
- 最佳干预窗口

**3. dsRNA 合成方案**
- 模板制备（引物、T7 添加、PCR 条件）
- IVT 反应（试剂盒、孵育、纯化）
- 质量控制标准

**4. 配方与递送方案**
- 配方方法（如适用）
- 递送分步说明（注射/饲喂/浸泡）
- 剂量、体积、浓度
- 暴露持续时间和条件

**5. 验证方案**
- RNA 提取方法
- cDNA 合成
- qPCR 参数和内参基因
- 表型观察指标和时间线

**6. 对照设计**
- 所有对照组及其理由
- 每组样本量

**7. 预期结果**
- 预测的敲低效率（基于 KB 证据或一般知识）
- 预期表型时间线
- 潜在陷阱

### 词汇规则

**允许使用：**
- 推荐、建议、典型、常见、可选
- "根据检索到的方案，推荐的注射体积为..."
- "如果 KB 中不包含物种特异性数据，则基于相关物种提供合理估计..."

**禁止使用：**
- 保证、总是、绝不、完美、最优（除非引用特定 KB 方案）
- 绝不断言特定的死亡率或敲低效率而未引用证据

### 边界情况：KB 证据不足

如果 `rnai_records` 和 `experiment_protocol` 都不包含匹配用户特定物种或基因的方案：
1. **第一备选**：执行在线文献搜索（PubMed/OpenAlex）寻找已发表的实验参数。
2. **第二备选**：使用 `rnai_records` 中亲缘物种（同属或同科）的方案作为参考。
3. 明确说明哪些参数来自 KB 证据、哪些来自在线文献、哪些是合理估计。
4. 添加注释："知识库中未找到物种特异性方案。参数基于 [亲缘物种 / 在线文献 / 一般原则] 估算。"

---

## 完整工作流示例

```
用户："为 Spodoptera frugiperda 设计靶向 CHS1 的注射 RNAi 实验。"

Agent 执行步骤：
1. 阶段 1 — 需求分析与多源 KB 搜索
   约束：species=S. frugiperda, gene=CHS1, delivery=注射, stage=幼虫

   a. 搜索 rnai_records 获取实验参数：
      search_knowledge(query="Spodoptera frugiperda CHS1 injection dsRNA concentration",
                       knowledge_base="rnai_records", top_k=10)
      → 返回 6 条记录：3 龄，1 µg/幼虫，85% 死亡率；4 龄，500 ng/幼虫，78% 死亡率...

   b. 搜索 experiment_protocol 获取工作流逻辑：
      search_knowledge(query="Spodoptera frugiperda larval injection dsRNA protocol",
                       knowledge_base="experiment_protocol", top_k=8)
      → 返回饲养方案、注射程序、qPCR 验证详情

   c. （可选）搜索在线最新文献：
      pubmed_Esearch(query="Spodoptera frugiperda CHS1 RNAi 2023 2024", max_results=5)
      → 返回 3 篇近期 PMID；用 pubmed_Efetch 获取详情

2. 阶段 2 — 生理背景分析
   → 从 rnai_records：大多数研究使用 3-4 龄幼虫
   → 饲养：26±1°C, 60-70% RH, 14L:10D, 人工饲料（来自 experiment_protocol）

3. 阶段 3 — dsRNA 设计
   → 模板：400 bp CHS1 区域，带 T7 悬臂
   → IVT：MEGAscript T7 试剂盒，37°C 4 h，LiCl 纯化
   → 从 rnai_records：每只幼虫 500 ng-1 µg，溶于 PBS（作为目标浓度）

4. 阶段 4 — 递送计划
   → 从 rnai_records：腹部节间注射，500 nL-1 µL 体积
   → 恢复：新鲜饲料上恢复 2 h，然后正常饲养

5. 阶段 5 — 验证与对照
   → 取样：注射后 24 h、48 h、72 h
   → qPCR：actin + GAPDH 内参，CHS1 引物
   → 对照：PBS 模拟、dsGFP、未处理
   → 表型：每日死亡率、体重、蜕皮缺陷

6. 报告生成
   → 完整结构化方案，参数来源清晰标注
   → 注明："注射剂量（500 ng-1 µg/幼虫）来自 rnai_records 中 S. frugiperda CHS1 的实验记录"
```

## 计算来源参考

| 组件 | 方法 |
|------|------|
| 实验参数检索 | `rnai_records` KB — Chroma + BM25 混合搜索 |
| 方案工作流检索 | `experiment_protocol` KB — Chroma + BM25 混合搜索 |
| 在线文献搜索 | PubMed E-utilities API + OpenAlex REST API |
| 方案综合 | 从多源 KB 内容 + 领域知识由 LLM 综合 |
