---
name: target_design_cn
category: rnai
description: 跨物种 RNAi 靶标设计分步指南 — 将已验证的近缘种引物序列映射到目标害虫基因组，重建扩增子，进行空间聚合、功能审计和连靶效应检测。
keywords: [靶标, 靶标设计, 跨物种, blast, 扩增子, 注释, mte, 连靶效应, 同源映射]
required_tools_tag: [target_discovery, sequence_processing]
---

# 跨物种 RNAi 靶标设计指南

## 概述

本指南涵盖紧接 `target_find` 之后的靶标设计工作流。`target_find` 确定**哪些基因**是有前景的 RNAi 靶标（基于亲缘关系和文献证据），而 `target_design` 将这些基因名转化为**目标害虫物种中具体的基因组坐标、序列和注释**。

**输入**（预期来自 `target_find` 的结果）：
- 目标害虫物种（如 `Spodoptera frugiperda`）
- 候选靶标基因（如 CHS1、V-ATPase）
- 近缘物种证据表 — 对每个验证过该靶标的近缘物种：
  - 近缘物种名称
  - 使用的引物序列（F/R）
  - 观察到的死亡率/表型
  - 参考文献（PMID）

**输出**：
- 验证过的扩增子序列（跨物种保守区域）
- 目标物种中的靶标坐标清单（含空间聚类）
- 功能注释（转录本 ID、基因描述）
- 靶标推荐指数
- 连靶效应（MTE）评估

工作流包含九个阶段：
1. **输入解析** — 从 target_find 结果中提取候选靶标和引物
2. **序列清洗** — 去除 T7 接头，保留 18-25nt 基因特异片段
3. **跨物种坐标映射** — BLAST 引物 → 注释 → 提取 CDS
4. **扩增子重建** — 根据引物坐标从 CDS 中裁剪验证过的扩增子
5. **同源映射到目标物种** — 用扩增子 BLAST 目标物种基因组
6. **空间聚合** — 按染色体分组、合并相邻簇
7. **功能审计** — 对每个靶标簇进行基因特征注释
8. **连靶效应检测（MTE）** — 检查 dsRNA 能否同时沉默多个必需基因
9. **报告生成** — 格式化靶标清单 + 推荐指数

### 先决条件

运行本工作流前，确认以下依赖：

- **系统软件**：NCBI BLAST+ (`blastn`)、BEDTools (`bedtools`)
  → 检查：`which blastn && which bedtools`
  → 安装：`sudo apt-get install ncbi-blast+ bedtools`

遵循 analysis-standards.skill 中的依赖检查协议。

---

## BLAST 参数策略

BLAST 参数必须根据输入序列长度选择，不同类型的序列需要不同的灵敏度/特异性权衡：

| 序列长度 | 场景 | word_size | evalue | gapopen | gapextend | penalty | reward |
|---------------|----------|-----------|--------|---------|----------|--------|-------|
| **< 100 nt**（引物、siRNA） | 跨物种映射（阶段 3） | 7 | 1.0 | 10（默认） | 4（默认） | -1（默认） | 1（默认） |
| **≥ 100 nt**（验证过的扩增子） | 同源映射到目标（阶段 5） | 7（默认） | 1e-10 | 5 | 2 | -1 | 1 |

**理由**：
- 短序列（<100nt）需要宽松的 evalue（1.0）和小 word_size（7），以跨物种找到远缘同源物。
- 长序列（≥100nt）使用严格的 evalue（1e-10）和调整过的空位罚分（gapopen=5, gapextend=2），以捕获完整的基因结构而非散片段。

---

## 阶段 1：输入解析

### 任务

从 `target_find` 的结果中提取结构化数据。用户应提供 target_find 报告（或引用该报告）。从中提取：

1. **目标物种** — 正在研究的害虫物种（拉丁双名）
2. **候选靶标基因列表** — 一个或多个基因及其功能分类
3. **近缘物种证据** — 对每个贡献证据的近缘物种：
   - 近缘物种名称（拉丁名）
   - 上游引物（Primer-F）和下游引物（Primer-R）序列
   - 死亡率/表型
   - 靶标基因名称（在该近缘物种中）
   - 参考文献（DOI 或 PMID）

### 靶标表结构

为每个候选靶标基因创建结构化证据表：

**基因：{靶标基因名} | 分类：{功能类别}**

| 近缘物种 | Primer-F (5'→3') | Primer-R (5'→3') | 死亡率 | 表型 | 参考文献 |
|----------------|-----------------|-----------------|-----------|-----------|-----------|
| _S. frugiperda_ | ATGGC... | TTAGGC... | 85% | 畸形蜕皮 | PMID: xxx |
| _S. exigua_ | ATGGC... | TTAAGC... | 72% | 生长停滞 | PMID: yyy |

### 边界情况

- **缺少引物序列**：如果 target_find 未包含引物序列（仅有基因名），工作流无法继续。要求用户从原始文献提供靶标基因的引物。
- **多个靶标基因**：每个靶标基因独立处理。对每个基因重复阶段 2-7。
- **每个基因有多对引物**：分别处理每对引物——它们可能靶向同一基因的不同区域，产生不同的扩增子。

---

## 阶段 2：序列清洗

### 任务

清洗用户提供的引物序列。文献中的引物常包含 T7 启动子序列、接头标签或其他非基因特异性的突出序列，必须在 BLAST 映射前去除。

### 工具：`clean_seq`

**参数：**
- `sequence`：原始引物序列（F 或 R）
- `seq_type`：`"dna"`

**输出中要关注的内容：**
- `cleaned_sequence`：归一化后的大写序列
- `length`：应为 18-25 nt（基因特异性引物）
- `gc_content`：用于质量检查（典型范围 40-60%）
- `normalization_notes`：所做的任何 U→T 转换

### 处理 T7 接头

许多 RNAi 引物包含用于体外转录的 T7 启动子序列：
- T7 启动子：`TAATACGACTCACTATAGGG`

如果引物以 `TAATACGACTCACTATAGGG` 开头：
1. 在调用 `clean_seq` 之前手动去除 T7 序列（前 20-22 nt）
2. 或对完整序列调用 `clean_seq`，然后用输出长度验证

### 结果处理

- **F 和 R 引物均已清洗**：进入阶段 3。
- **引物清洗后过短（<15 nt）**：警告用户——可能无法产生特异性 BLAST 结果。
- **引物包含无效字符**：报告给用户并标注位置。

---

## 阶段 3：跨物种坐标映射

### 任务

对每个近缘物种，将清洗后的引物映射到基因组以定位靶标基因。这是三步子工作流：

1. **BLAST** — 将每对引物比对到近缘物种基因组
2. **注释** — 找到与 BLAST 结果重叠的转录本/基因
3. **提取 CDS** — 检索识别到的转录本的完整 CDS 序列

### 步骤 3a：BLAST 引物

### 工具：`insect_blast`

**参数（短序列模式 — 引物 < 100 nt）：**
- `sequence`：清洗后的引物序列
- `species`：近缘物种名称（拉丁名，下划线分隔）
- `word_size`：`7`
- `evalue`：`1.0`
- `gapopen`：`10`（默认）
- `gapextend`：`4`（默认）

**分别对 F 和 R 引物运行 BLAST。** 每对引物应映射到同一基因座（F 靠近靶标区域 5' 端，R 靠近 3' 端）。

**输出含义：**
- `hits_count`：基因组匹配数
- 每条 hit 包含：`subject_id`（染色体/支架）、`identity_pct`、`aln_length`、`s_start`、`s_end`
- 对于特异性引物，最佳 hit 通常具有最高的 `identity_pct` 和 `bit_score`

### 步骤 3b：注释 BLAST 结果

### 工具：`insect_anno`

**参数：**
- `blast_hits`：每对引物的最佳 BLAST hit。同时包含 F 和 R 引物的 hits。
  - 每条 hit 需要：`chromosome`、`start_position`、`end_position`、`name`（如 "CHS1_F_primer"）、`strand`
- `species`：近缘物种名称
- `window_size`：`100`（默认，足以捕获附近的基因特征）

**输出中要关注的内容：**
- `hits_annotated`：成功注释的 hit 数
- `feature_types`：找到的特征类型（gene、mRNA、CDS、exon、UTR）
- `results` 中的每条 feature 包含：
  - `type`：特征类型
  - `attributes`：特征属性，包括 `ID`、`Parent` 以及 `gene`/`product` 描述

**如何选择正确的转录本：**
- 如果存在多个异构体，优先选择功能描述与靶标基因名匹配的转录本。
- 在 feature 属性中查找与已知基因功能匹配的 `gene` 或 `product` 描述。
- 如果注释中包含 `transcript_id` 或 `mRNA_ID`，记下以备下一步使用。

### 步骤 3c：提取 CDS 序列

### 工具：`fetch_insect_cds`

**参数：**
- `species`：近缘物种名称
- `transcript_id`：从步骤 3b 注释中获得的转录本 ID

**输出含义：**
- `status`："success"（找到转录本）
- `length`：CDS 长度（bp）
- `sequence`：完整 CDS 核苷酸序列（5'→3'，正义链）

### 结果处理

- **两条引物映射到同一基因**：进入阶段 4。
- **引物映射到不同基因/染色体**：可能表示引物非特异性或基因组组装问题。标记此问题——不对这对引物进行扩增子重建。
- **引物无 BLAST 结果**：尝试放宽参数（evalue: 10, word_size: 6）。如仍无结果，标记该引物对为"不可映射"。
- **CDS 数据库中未找到转录本 ID**：注释 ID 格式可能与 CDS FASTA 头格式不同。尝试按基因名或附近基因坐标搜索。

---

## 阶段 4：扩增子重建

### 任务

利用完整的 CDS 序列和映射到的引物坐标，提取验证过的扩增子——即 F 引物 5' 端到 R 引物 3' 端之间的序列。

### 工具：`clip_seq`

**参数：**
- `sequence`：完整 CDS 序列（来自阶段 3c）
- `start`：F 引物匹配在 CDS 上的 5' 坐标
- `length`：从 F 引物起始到 R 引物末端的距离
  - 公式：`length = R_primer_end - F_primer_start + 1`
- `as_reverse_complement`：`False`（保持正义链）

### 命名规范

每个验证过的扩增子必须按以下格式命名：

```
{物种缩写}-{基因功能}-{编号}
```

其中：
- **物种缩写**：属名首字母 + 种名前 3 个字母（如 `Sfru` 对应 _Spodoptera frugiperda_，`Dmel` 对应 _Drosophila melanogaster_）
- **基因功能**：基因功能缩写（如 `CHS` 对应几丁质合酶，`VAT` 对应 V-ATPase）
- **编号**：顺序编号（01, 02, ...），用于同一基因的多个扩增子

示例：`Sfru-CHS-01`、`Sfru-VAT-01`、`Sexi-CHS-01`

### 结果处理

- **扩增子长度检查**：典型 dsRNA 片段为 200-500 bp。如扩增子 <100 bp，可能过短难以有效 RNAi。如 >1000 bp，可能难以合成。
- **GC 含量**：30-65% 可接受。标记极端值。
- **扩增子超出 CDS 边界**：警告用户——扩增子可能包含 UTR 区域。

---

## 阶段 5：同源映射到目标物种

### 任务

将验证过的扩增子（来自阶段 4）作为查询序列，BLAST 到**目标害虫物种**基因组。这确定了目标物种中同源序列的位置。

### 工具：`insect_blast`

**参数（长序列模式 — 扩增子 ≥ 100 nt）：**
- `sequence`：验证过的扩增子序列（来自阶段 4）
- `species`：目标害虫物种名称
- `word_size`：`7`（默认）
- `evalue`：`1e-10`（严格，以捕获完整基因结构）
- `gapopen`：`5`（降低以允许内含子间隙）
- `gapextend`：`2`（降低以允许内含子间隙）
- `penalty`：`-1`
- `reward`：`1`

**输出中要关注的内容：**
- `hits_count`：目标基因组中的显著匹配数
- 每条 hit：
  - `subject_id`：染色体/支架名称
  - `identity_pct`：序列相似性——越高越好
  - `aln_length`：比对长度
  - `s_start`、`s_end`：染色体上的基因组坐标

### 结果处理

- **找到高一致性 hit（≥80%）**：同源性强证据。进入阶段 6。
- **中等一致性 hit（60-80%）**：可能同源但有分歧。纳入分析但标注较低置信度。
- **未找到 hit**：扩增子在目标物种中可能不保守。尝试放宽 evalue 至 1e-5。如仍无结果，标记该靶标为"不可映射"。

---

## 阶段 6：空间聚合

### 任务

将阶段 5 的所有 BLAST 结果组织成结构化的空间图谱。按染色体分组，合并相邻簇，检测潜在的多拷贝基因。

### 规则（LLM 推理 — 无需工具）

**第 1 步 — 按染色体分组**
按 `subject_id`（染色体/支架）对所有结果分组。每条染色体一个组。

**第 2 步 — 按位置排序**
在每个染色体组内，按基因组起始位置排序。

**第 3 步 — 合并相邻 hit**
对同一染色体上的连续 hit：
- 计算间隙：`gap = next_hit_start - current_hit_end`
- 若 `gap < 2000 bp`：合并到同一**簇**（可能属于同一基因的不同外显子）
- 若 `gap ≥ 2000 bp`：保留为独立簇（可能的多拷贝基因或不同位点）

**第 4 步 — 确定聚类范围**
对每个合并的簇，报告**完整聚类范围**：
- `cluster_start`：簇中第一个 hit 的起始位置
- `cluster_end`：簇中最后一个 hit 的结束位置
- `strand`：从 BLAST hit 链信息推断

### 输出格式

**染色体：{subject_id}**

| 簇 | Hit 数 | 簇起始 | 簇结束 | 链 | 一致性范围 (%) | 备注 |
|---------|-----------|--------------|------------|--------|-------------------|-------|
| 1 | 3 | 1,234,500 | 1,238,200 | + | 85.2 - 92.1 | 合并 3 个 hit（gap < 2kb） |
| 2 | 1 | 2,456,000 | 2,456,300 | - | 78.5 | 孤立 hit |

**如果不同染色体上存在多个一致性水平相似的簇**，注明：
> ⚠️ 检测到多个同源区域。这表示目标物种中可能存在多拷贝基因或假基因。

---

## 阶段 7：功能审计

### 任务

使用目标物种基因组注释对每个靶标簇进行基因特征注释。提供每个靶标区域的转录本 ID、基因描述和功能背景。

### 工具：`insect_anno`

**参数：**
- `blast_hits`：对阶段 6 的每个簇，创建一个 BlastHitInput：
  - `chromosome`：subject_id
  - `start_position`：cluster_start
  - `end_position`：cluster_end
  - `name`：如 "CHS1_cluster1"
  - `strand`：从 BLAST 推断
- `species`：目标害虫物种名称
- `window_size`：`100`

### 关注内容

每个簇的注释揭示：
- **Gene ID**：目标物种中的靶标基因标识符
- **Transcript ID**：特定转录本异构体
- **Gene description**：基因的功能注释
- **Feature types**：区域覆盖 CDS、UTR 还是非编码特征

### 注释表

| 簇 | Gene ID | Transcript ID | 描述 | 特征类型 | 链 | 置信度 |
|---------|--------|--------------|-------------|---------------|--------|-----------|
| 1 | gene001234 | mRNA_001 | 几丁质合酶 1 | CDS, exon | + | 高（92% 一致性） |
| 2 | gene005678 | mRNA_005 | 几丁质合酶样 | CDS, exon | - | 中等（78%） |

### 结果处理

- **簇注释到预期基因**：高置信度靶标。进入推荐指数计算。
- **簇注释到不同基因**：扩增子可能交叉映射到旁系同源物。标记需注意。
- **未找到注释**：该区域可能是基因间区。较低置信度——在报告中注明。

---

## 阶段 8：连靶效应检测（MTE）

### 任务

评估给定的 dsRNA 序列（通常是验证过的扩增子）是否能同时干扰目标物种中的**多个必需基因**。连靶效应可以通过同时靶向多个基因增强 RNAi 农药效果。

### 触发条件

此阶段为可选项。在以下情况下执行：
- 用户明确要求进行连靶效应分析。
- 扩增子序列中度保守（可能匹配多个位点）。

### 第 1 步：BLAST 扩增子

### 工具：`insect_blast`

**参数：**
- `sequence`：验证过的扩增子序列
- `species`：目标害虫物种名称
- `word_size`：`7`（默认）
- `evalue`：`1e-5`（稍微放宽以捕获中等匹配）
- `gapopen`：`5`
- `gapextend`：`2`

**过滤结果**：保留 `identity_pct ≥ 80` 的 hits 用于进一步分析。

### 第 2 步：注释高置信度 Hit

### 工具：`insect_anno`

使用与阶段 7 相同的方法注释每个符合条件的 hit。

### 第 3 步：MTE 评估

分析注释后的 hits，确定：

1. **Hit 数量**：dsRNA 匹配多少个不同的基因座？
   - 1 个基因座 → 单靶标（无连靶效应）
   - 2+ 个基因座 → 潜在连靶效应

2. **功能多样性**：hit 的基因功能相关还是多样？
   - 同一通路 → 可能协同效应
   - 不同通路 → 可能广谱效应
   - 必需基因（看家）→ 更高致死潜力

3. **一致性分布**：次级 hit 是否与主要 hit 几乎一样强？
   - 次级 hit 一致性 >85% → 强连靶潜力
   - 次级 hit 一致性 80-85% → 中等潜力
   - 次级 hit 一致性 <80% → 弱潜力

### MTE 分类

| 类别 | 标准 | 建议 |
|----------|----------|---------------|
| **强 MTE** | ≥2 个必需基因，>85% 一致性 | 推荐作为多靶向 RNAi 农药候选 |
| **中等 MTE** | ≥2 个基因，80-85% 一致性 | 建议进一步实验验证 |
| **单靶标** | 仅 1 个基因或次级 hit <80% | 报告为单靶标候选 |

---

## 阶段 9：报告生成

### 任务

生成全面的靶标设计报告。报告必须包含以下内容：

### 报告结构

**1. 输入摘要**
- 目标物种：[拉丁名]
- 来源：target_find 结果引用
- 处理的候选基因：[列表]

**2. 验证过的扩增子表**

| 扩增子 ID | 来源物种 | 基因 | 长度 (bp) | GC% | Primer-F | Primer-R |
|-------------|---------------|------|-------------|-----|----------|----------|
| Sfru-CHS-01 | _S. frugiperda_ | CHS1 | 385 | 42.3 | ATCG... | GCTA... |

**3. 目标物种 Hit 图谱**

对每个候选基因，空间聚合表（来自阶段 6）：
- 染色体、簇范围、链、hit 数、一致性范围

**4. 功能注释表**（来自阶段 7）

| 靶标基因 | 靶标簇 | Gene ID | Transcript ID | 描述 |
|-------------|---------------|---------|--------------|-------------|
| CHS1 | chr1:1,234,500-1,238,200 | gene001234 | mRNA_001 | 几丁质合酶 1 |

**5. 推荐指数**

基于以下维度为每个候选靶标计算推荐指数：

| 因素 | 权重 | 评分标准 |
|--------|--------|---------|
| BLAST 一致性（目标物种） | 高 | ≥90%=5, 85-90%=4, 80-85%=3, <80%=2 |
| 近缘种死亡率 | 高 | ≥80%=5, 60-80%=4, 40-60%=3, <40%=2 |
| 跨物种保守度 | 中 | ≥3 个近缘种=5, 2=4, 1=3 |
| 功能必需性 | 中 | 看家=5, 通路=4, 组织特异=3 |
| 靶标簇质量 | 低 | 单簇=5, 同染色体多簇=4, 多染色体=3 |

**总分**：加权得分之和。以星级（★）或数值评分呈现。

| 靶标基因 | 一致性 | 死亡率 | 保守度 | 必需性 | 簇质量 | **总分** |
|-------------|----------|-----------|-------------|-------------|---------|-----------|
| CHS1 | 5 | 5 | 5 | 4 | 5 | **★★★★★** |
| V-ATPase | 4 | 4 | 4 | 5 | 4 | **★★★★** |

**6. 连靶效应评估**（如已执行）

| 扩增子 | Hit 数 | 靶标基因 | MTE 类别 | 建议 |
|----------|------|-------------|-------------|---------------|
| Sfru-CHS-01 | 1 | 仅 CHS1 | 单靶标 | 按单靶标设计进行 |
| Sfru-VAT-01 | 3 | VAT-A, VAT-B, VAT-D | 强 MTE | 推荐作为多靶向候选 |

**7. 建议**
- 最优先的 dsRNA 设计靶标
- 需要进一步验证的靶标
- 建议的下一步工作流（dsRNA 设计 → 安全检测 → 递送设计）

### 词汇规则

**允许使用：**
- 建议、推荐、候选、优先、预测
- "CHS1 被推荐作为 dsRNA 设计的最高优先级靶标"
- "该扩增子基于 92% BLAST 一致性预测可映射到目标物种"

**禁止使用：**
- 证明、确认、保证、有效（未经实验验证）
- 绝不可仅基于计算映射断言体内 RNAi 效果

### 结束语

每个靶标设计周期结束时输出：
> "RNAi target mining and mapping completed."

或对于 MTE 分析：
> "Multi-target effect analysis completed."

---

## 完整工作流示例

```
用户提供 target_find 结果，显示：
- 目标：Spodoptera frugiperda
- 候选：CHS1（几丁质合酶）
- 证据：S. frugiperda（引物：ATGGC.../TTAGGC...，死亡率 85%）
            S. exigua（引物：ATGGC.../TTAAGC...，死亡率 72%）

Agent 执行步骤：

1. 阶段 1 — 输入解析
   → 为 CHS1 创建证据表，包含两个物种的引物和死亡率

2. 阶段 2 — 序列清洗
   → clean_seq(sequence="TAATACGACTCACTATAGGGATGGC...", seq_type="dna")
   → 先手动去除 T7 接头，再清洗
   → 对所有 4 条引物重复（2 个物种 × 2 条引物）

3. 阶段 3 — 跨物种坐标映射
   对 S. frugiperda：
   → insect_blast(sequence="ATGGC...", species="Spodoptera_frugiperda",
                   word_size=7, evalue=1.0)
   → insect_anno(blast_hits=[F_hit, R_hit], species="Spodoptera_frugiperda")
   → fetch_insect_cds(species="Spodoptera_frugiperda",
                       transcript_id="mRNA_001234")
   → 获得完整 CDS：1650 bp 序列
   对 S. exigua 重复

4. 阶段 4 — 扩增子重建
   → clip_seq(sequence=CDS, start=F_start, length=R_end-F_start+1)
   → 命名：Sfru-CHS-01（385 bp），Sexi-CHS-01（378 bp）

5. 阶段 5 — 同源映射到目标
   → insect_blast(sequence=Sfru-CHS-01 序列, species="Spodoptera_frugiperda",
                   evalue=1e-10, gapopen=5, gapextend=2)
   → 在染色体 1 上找到 5 个 hit

6. 阶段 6 — 空间聚合
   → 按 chr1 分组：5 个 hit，间隙 < 2000bp → 合并为 1 个簇
   → 簇范围：chr1:1,234,500-1,238,200

7. 阶段 7 — 功能审计
   → insect_anno(blast_hits=[cluster1_hit], species="Spodoptera_frugiperda")
   → 基因：CHS1，转录本：mRNA_001，描述：几丁质合酶 1

8. 阶段 8 — MTE 检测
   → insect_blast(sequence=扩增子, species="Spodoptera_frugiperda",
                   evalue=1e-5)
   → 1 个高置信度 hit → 单靶标，无 MTE

9. 阶段 9 — 报告生成
   → 输出包含所有表格和推荐指数的结构化报告
```

## 计算来源参考

| 组件 | 方法 |
|-----------|--------|
| 序列清洗 | `clean_seq` — 归一化、T/U 转换、验证 |
| 跨物种 BLAST | `insect_blast` — 短序列参数（word_size=7, evalue=1.0） |
| 基因组注释 | `insect_anno` — bedtools window 对 GFF3 |
| CDS 检索 | `fetch_insect_cds` — 按转录本 ID 搜索 FASTA |
| 扩增子裁剪 | `clip_seq` — 基于坐标的子序列提取 |
| 同源映射 | `insect_blast` — 长序列参数（evalue=1e-10, gapopen=5） |
| 空间聚合 | LLM 推理 — 染色体分组 + 2000bp 合并阈值 |
| 功能审计 | `insect_anno` 对目标物种基因组 |
| MTE 检测 | `insect_blast` + `insect_anno` 放宽 evalue（1e-5） |
| 推荐指数 | LLM 综合 — 一致性 × 死亡率 × 保守度 × 必需性 |
